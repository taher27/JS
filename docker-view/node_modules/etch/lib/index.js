const dom = require('etch/lib/dom')
const render = require('etch/lib/render')
const {initialize, update, updateSync, destroy, destroySync} = require('etch/lib/component-helpers')
const {setScheduler, getScheduler} = require('etch/lib/scheduler-assignment')

module.exports = {
  dom, render,
  initialize, update, updateSync, destroy, destroySync,
  setScheduler, getScheduler
}
